import type { QuizAnswers } from "../types";

// Placeholder scoring. Replace once the real JSON scoring rules are provided.
export function computeQuizScore(_answers: QuizAnswers): number {
  return 0;
}
